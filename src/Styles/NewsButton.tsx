import { StyleSheet } from "react-native";

const NewsButton_Styeled = StyleSheet.create({
    Button: {
        backgroundColor: "#96B8FF",
        borderRadius: 3,
        margin: 5,
        padding: 5,
    },
    ButtonText: {
        color: '#fff',
        paddingVertical: 10
    }
})

export default NewsButton_Styeled